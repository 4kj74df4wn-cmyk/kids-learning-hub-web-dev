const APP_VERSION='0.5.1-dev';
const CONTENT_VERSION='math-core-loop-2026-08-31-v2';
const EVENT_SCHEMA_VERSION='1.4';
const KEY='KLH_PUBLIC_WEB_DEV_V05_TESTER';
const LEGACY_KEYS=['KLH_PUBLIC_WEB_DEV_V04_TESTER','KLH_PUBLIC_WEB_DEV_V03_TESTER'];

const SKILLS=[
  {
    id:'division', title:'การหารด้วยตัวหาร 2 หลัก',
    lesson:'ประมาณคำตอบก่อน แล้วใช้การคูณกลับตรวจ เช่น 3,600 ÷ 24 = 150 เพราะ 24 × 150 = 3,600',
    visual:'24 × 150 = 3,600 ⇄ 3,600 ÷ 24 = 150',
    url:'https://proj14.ipst.ac.th/p4/p4-math-book1/math-p4b1-031/',
    diagnostic:{id:'DEV_MATH_DIV_DIAG_001',p:'1,440 ÷ 24 = ?',c:['50','60','70','80'],a:1,wrong:{0:['calculation','คำนวณคลาดเคลื่อน','ลองคูณคำตอบกลับด้วย 24'],2:['estimation','ประเมินขนาดคำตอบสูงไป','24 × 60 = 1,440 พอดี'],3:['estimation','ประเมินขนาดคำตอบสูงไป','ลองเทียบ 24 × 80 กับ 1,440']}},
    fresh:[{id:'DEV_MATH_DIV_001',p:'2,880 ÷ 24 = ?',c:['100','110','120','140'],a:2},{id:'DEV_MATH_DIV_002',p:'4,320 ÷ 27 = ?',c:['120','140','160','180'],a:2},{id:'DEV_MATH_DIV_003',p:'5,250 ÷ 25 = ?',c:['180','200','210','225'],a:2}],
    rescue:{id:'DEV_MATH_DIV_RESCUE_001',p:'3,360 ÷ 24 = ?',c:['120','130','140','150'],a:2,hints:['เริ่มจากประมาณ 3,360 ÷ 24 ว่าควรอยู่แถว 100 กว่า','ลองคูณกลับ: 24 × 140 = 3,360']},
    retest:{id:'DEV_MATH_DIV_POST_001',p:'4,080 ÷ 24 = ?',c:['150','160','170','180'],a:2}
  },
  {
    id:'fraction', title:'เศษส่วนของเศษส่วน',
    lesson:'คำว่า “ของ” ให้คิดเป็นการคูณ เช่น 1/4 ของ 3/5 = 1/4 × 3/5',
    visual:'1/4 ของ 3/5 = 3/20',
    url:'https://proj14.ipst.ac.th/p5/p5-math-book1/math-p5b1-014/',
    diagnostic:{id:'DEV_MATH_FRAC_DIAG_001',p:'2/3 ของ 3/4 เท่ากับเท่าไร?',c:['1/4','1/3','1/2','2/3'],a:2,wrong:{0:['procedure','คูณเศษส่วนไม่ครบ','คำว่า “ของ” ให้คูณ 2/3 × 3/4'],1:['concept','อาจสับสนการคูณกับการหาร','คูณเศษกับเศษ และส่วนกับส่วน'],3:['concept','อาจเลือกค่าที่ดูคุ้นโดยไม่คำนวณ','เขียน 2/3 × 3/4 ก่อนตัดทอน']}},
    fresh:[{id:'DEV_MATH_FRAC_001',p:'มีน้ำ 4/5 ถัง ใช้ไป 1/2 ของน้ำที่มี เหลือเท่าไร?',c:['1/5','2/5','3/5','4/5'],a:1},{id:'DEV_MATH_FRAC_002',p:'มีเค้ก 3/4 ก้อน กินไป 1/3 ของที่มี กินไปเท่าไร?',c:['1/4','1/3','1/2','2/3'],a:0},{id:'DEV_MATH_FRAC_003',p:'มีน้ำ 5/6 ถัง ใช้ไป 2/5 ของที่มี เหลือเท่าไร?',c:['1/3','1/2','3/5','2/3'],a:1}],
    rescue:{id:'DEV_MATH_FRAC_RESCUE_001',p:'มีน้ำ 3/4 ถัง ใช้ไป 2/3 ของน้ำที่มี ใช้ไปเท่าไร?',c:['1/3','1/2','2/3','3/4'],a:1,hints:['คำว่า “2/3 ของ 3/4” แปลเป็น 2/3 × 3/4','2/3 × 3/4 = 6/12 แล้วตัดทอน']},
    retest:{id:'DEV_MATH_FRAC_POST_001',p:'4/5 ของ 5/8 เท่ากับเท่าไร?',c:['2/5','1/2','3/5','4/5'],a:1}
  },
  {
    id:'decimal', title:'หารทศนิยมด้วยทศนิยม',
    lesson:'ทำตัวหารให้เป็นจำนวนเต็ม โดยเลื่อนจุดทศนิยมของทั้งตัวตั้งและตัวหารเท่ากัน',
    visual:'2.4 ÷ 0.15 → 240 ÷ 15 = 16',
    url:'https://proj14.ipst.ac.th/p6/p6-math-book1/math-p6b1-029/',
    diagnostic:{id:'DEV_MATH_DEC_DIAG_001',p:'2.4 ÷ 0.3 = ?',c:['0.8','8','80','800'],a:1,wrong:{0:['place_value','ตำแหน่งทศนิยมคลาดเคลื่อน','เลื่อนจุดของทั้ง 2.4 และ 0.3 หนึ่งตำแหน่ง'],2:['place_value','คำตอบมากเกินไป 10 เท่า','24 ÷ 3 = 8 ไม่ใช่ 80'],3:['place_value','คำตอบมากเกินไป 100 เท่า','ทำตัวหารเป็นจำนวนเต็มก่อน']}},
    fresh:[{id:'DEV_MATH_DEC_001',p:'1.8 ÷ 0.2 = ?',c:['6','7','8','9'],a:3},{id:'DEV_MATH_DEC_002',p:'3.6 ÷ 0.12 = ?',c:['20','25','30','36'],a:2},{id:'DEV_MATH_DEC_003',p:'4.95 ÷ 0.15 = ?',c:['30','32','33','35'],a:2}],
    rescue:{id:'DEV_MATH_DEC_RESCUE_001',p:'6.4 ÷ 0.16 = ?',c:['4','20','40','400'],a:2,hints:['เลื่อนจุด 2 ตำแหน่งทั้งคู่: 640 ÷ 16','16 × 40 = 640']},
    retest:{id:'DEV_MATH_DEC_POST_001',p:'7.2 ÷ 0.18 = ?',c:['4','20','40','80'],a:2}
  }
];

function blank(){return{profile:null,done:{},results:{},skillState:{},attemptHistory:[],events:[]}}
function normalize(s){const results={};Object.entries(s?.results||{}).forEach(([skill,r])=>{if(r&&r.freshScore!=null)results[skill]=r;else if(r&&r.score!=null)results[skill]={freshScore:r.score,rescueScore:null,postSupportFreshScore:null,avgConfidence:r.confidence??null,at:r.at||null,label:'MIGRATED_LEGACY_DEV'}});return{profile:s?.profile||null,done:s?.done||{},results,skillState:s?.skillState||{},attemptHistory:Array.isArray(s?.attemptHistory)?s.attemptHistory:[],events:Array.isArray(s?.events)?s.events:[]}}
function loadState(){try{const now=JSON.parse(localStorage.getItem(KEY)||'null');if(now)return normalize(now)}catch(e){}for(const legacyKey of LEGACY_KEYS){try{const old=JSON.parse(localStorage.getItem(legacyKey)||'null');if(old){const m=normalize(old);m._migratedFrom=legacyKey;localStorage.setItem(KEY,JSON.stringify(m));return m}}catch(e){}}return blank()}
let state=loadState();let active=null,phase='idle',idx=0,answers=[],conf=[],itemStartAt=0,diagnosticEvidence=null,wrongEvidence=[],rescueHintLevel=0,currentAttemptId=null,attemptStartedAt=null;
function save(){localStorage.setItem(KEY,JSON.stringify(state))}
function log(type,extra={}){state.events.push({ts:new Date().toISOString(),type,appVersion:APP_VERSION,contentVersion:CONTENT_VERSION,eventSchemaVersion:EVENT_SCHEMA_VERSION,attemptId:currentAttemptId,scenario:state.profile?.scenario||null,tester:state.profile?.tester||null,...extra});save()}
if(state._migratedFrom){const from=state._migratedFrom;delete state._migratedFrom;log('state_migrated',{fromKey:from,toKey:KEY})}
function hideAll(){['setup','home','learn','telemetry'].forEach(x=>document.getElementById(x).classList.add('hidden'))}
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function nowIso(){return new Date().toISOString()}
function retentionSchedule(){const base=Date.now(),days=[1,3,7,21];return days.map(d=>({day:d,dueAt:new Date(base+d*86400000).toISOString(),status:'PENDING'}))}
function deriveSkillStatus(freshScore,rescueScore,postSupportFreshScore){if(freshScore===100)return'TENTATIVE_MASTERY_DEV';if(postSupportFreshScore===100)return'TRANSFER_AFTER_SUPPORT_DEV';return'NEEDS_FOUNDATION_REPAIR'}
function userStateText(status){return status==='TENTATIVE_MASTERY_DEV'?'รอบนี้เข้าใจได้ดี':status==='TRANSFER_AFTER_SUPPORT_DEV'?'ดีขึ้นหลังทบทวน และทำโจทย์ใหม่ได้แล้ว':'ยังมีพื้นฐานบางจุดที่ต้องฝึกเพิ่ม'}
function userStateEmoji(status){return status==='TENTATIVE_MASTERY_DEV'?'🎯':status==='TRANSFER_AFTER_SUPPORT_DEV'?'💪':'🧩'}
function diagnosisThai(d){const map={calculation:'คำนวณยังไม่แม่น',estimation:'การกะขนาดคำตอบยังคลาดเคลื่อน',procedure:'ขั้นตอนการทำยังไม่แม่น',concept:'แนวคิดบางจุดยังสับสน',place_value:'ตำแหน่งทศนิยมยังหลอกเราได้',misconception_risk:'อาจมีแนวคิดบางจุดที่เข้าใจคลาดเคลื่อน',strategy_or_processing_gap:'ยังเลือกวิธีทำได้ไม่คล่อง',knowledge_or_prerequisite_gap:'อาจมีพื้นฐานก่อนหน้านี้ที่ต้องเติม',procedural_gap:'ขั้นตอนการทำยังไม่คล่อง',low_confidence_correct:'ทำถูกแล้ว แต่ยังไม่ค่อยมั่นใจ',no_error_observed:'ยังไม่พบจุดติดจากข้อนี้'};return map[d.type]||'มีจุดหนึ่งที่เราน่าลองแกะเพิ่ม'}
function devDetails(html){return `<details class="devdetails"><summary>รายละเอียดสำหรับ DEV</summary><div class="devbox">${html}</div></details>`}
function enterApp(){state.profile={tester:document.getElementById('tester').value.trim()||'DEV Tester',scenario:document.getElementById('scenario').value};log('session_start');renderHome()}
function renderHome(){hideAll();document.getElementById('home').classList.remove('hidden');document.getElementById('hello').textContent='พร้อมลุยกันต่อไหม?';document.getElementById('scenarioText').textContent='วันนี้เราแข่งกับโจทย์และตัวเราเอง — ไม่แข่งกับใคร';document.getElementById('completedKpi').textContent=Object.keys(state.done).length;document.getElementById('eventKpi').textContent=state.attemptHistory.length;document.getElementById('modeKpi').textContent='เรียน → ลอง → แก้จุดติด';document.getElementById('versionNote').textContent='App '+APP_VERSION+' • Content '+CONTENT_VERSION+' • Event schema '+EVENT_SCHEMA_VERSION+' • events '+state.events.length;document.getElementById('queue').innerHTML=SKILLS.map(s=>{const r=state.results[s.id],ss=state.skillState[s.id];let detail='ยังไม่ได้ลองหัวข้อนี้';if(r){if(r.freshScore===100)detail='รอบล่าสุด: ทำโจทย์ใหม่ได้ครบด้วยตัวเอง 🎯';else if(r.postSupportFreshScore===100)detail='รอบล่าสุด: หลังทบทวนแล้วทำโจทย์ใหม่ได้ 💪';else detail='รอบล่าสุด: เจอจุดที่ต้องฝึกต่อ — ดีเลย เรารู้ว่าจะซ่อมตรงไหน 🧩'}const status=ss?`<div class="state">${userStateEmoji(ss.status)} <strong>${esc(userStateText(ss.status))}</strong></div>`:'';return `<div class="module"><strong>${esc(s.title)}</strong><div class="progress"><div style="width:${state.done[s.id]?'100':'0'}%"></div></div><div class="muted small">${detail}</div>${status}<div class="actions"><button class="btn primary" onclick="start('${s.id}')">${state.done[s.id]?'ลองเอาชนะรอบเก่า':'เริ่มภารกิจ'}</button></div>${r?devDetails(`Fresh ${r.freshScore}% • Rescue ${r.rescueScore==null?'n/a':r.rescueScore+'%'} • Post ${r.postSupportFreshScore==null?'n/a':r.postSupportFreshScore+'%'} • ${ss?.status||'no state'}`):''}</div>`}).join('')}
function start(id){active=SKILLS.find(s=>s.id===id);phase='diagnostic';idx=0;answers=[];conf=[];diagnosticEvidence=null;wrongEvidence=[];rescueHintLevel=0;delete state._pending;currentAttemptId='DEV-'+active.id+'-'+Date.now();attemptStartedAt=nowIso();log('module_start',{skill:id});hideAll();document.getElementById('learn').classList.remove('hidden');beginItem()}
function beginItem(){itemStartAt=Date.now();renderCurrentItem()}
function currentQuestion(){if(phase==='diagnostic')return active.diagnostic;if(phase==='fresh')return active.fresh[idx];if(phase==='rescue')return active.rescue;if(phase==='post_support')return active.retest;return null}
function phaseLabel(){return phase==='diagnostic'?'เช็กพื้นฐานสั้นๆ ก่อน':phase==='fresh'?`ลองโจทย์ใหม่ด้วยตัวเอง ${idx+1} / ${active.fresh.length}`:phase==='rescue'?'มาแกะจุดที่ยังติดกัน':'พิสูจน์กันอีกข้อ — คราวนี้ทำเองล้วนๆ'}
function phaseCoach(){if(phase==='diagnostic')return'ไม่ต้องกดดัน ข้อนี้แค่ช่วยให้เรารู้ว่าจะสอนตรงไหน';if(phase==='fresh')return'ลองด้วยตัวเองเต็มที่ ผิดได้ เพราะข้อผิดนี่แหละบอกทางให้เรา';if(phase==='rescue')return'โจทย์ตัวแสบโผล่มาแล้ว 😄 เราค่อยๆ แกะมันทีละชั้น';return'ด่านพิสูจน์! ไม่มีตัวช่วยแล้ว มาดูกันว่าวิธีเมื่อกี้ติดมือหรือยัง 💪'}
function renderCurrentItem(){const q=currentQuestion(),sel=answers[idx],cv=conf[idx];const hintBlock=phase==='rescue'&&rescueHintLevel>0?`<div class="hint"><strong>คำใบ้ ${rescueHintLevel}</strong><br>${esc(q.hints[rescueHintLevel-1])}</div>`:'';const hintButtons=phase==='rescue'?`<div class="actions">${rescueHintLevel<1?'<button class="btn light" onclick="showHint(1)">ขอคำใบ้เบาๆ 💡</button>':''}${rescueHintLevel<2?'<button class="btn light" onclick="showHint(2)">ขออีกนิด 😄</button>':''}</div>`:'';document.getElementById('pane').innerHTML=`<div class="muted">${phaseLabel()}</div><div class="coach">${phaseCoach()}</div><h2>${esc(q.p)}</h2><div class="choices">${q.c.map((x,i)=>`<button class="choice ${sel===i?'sel':''}" onclick="pick(${i})">${String.fromCharCode(65+i)}. ${esc(x)}</button>`).join('')}</div><div class="conf"><span class="muted small" style="align-self:center">ตอนตอบรู้สึกแค่ไหน?</span><button class="c ${cv===1?'on':''}" onclick="setc(1)">เดาสุดๆ 😅</button><button class="c ${cv===2?'on':''}" onclick="setc(2)">พอมั่นใจ 🙂</button><button class="c ${cv===3?'on':''}" onclick="setc(3)">มั่นใจเลย 💪</button></div>${hintBlock}${hintButtons}<div class="actions"><button class="btn primary" onclick="submitCurrent()">${phase==='fresh'&&idx<active.fresh.length-1?'ไปข้อต่อไป →':'ส่งคำตอบ'}</button><button class="btn light" onclick="exitModule()">พักก่อน</button></div>${devDetails(`phase=${phase} • question_id=${q.id} • skill=${active.id}`)}`}
function pick(i){answers[idx]=i;renderCurrentItem()}
function setc(v){conf[idx]=v;renderCurrentItem()}
function showHint(level){rescueHintLevel=Math.max(rescueHintLevel,level);log('hint_open',{skill:active.id,questionId:active.rescue.id,phase:'rescue',hintLevel:level});renderCurrentItem()}
function exitModule(){if(active&&phase!=='completed')log('module_abandon',{skill:active.id,phase,questionId:currentQuestion()?.id||null});delete state._pending;active=null;phase='idle';currentAttemptId=null;attemptStartedAt=null;save();renderHome()}
function diagnose(q,selected,correct,confidence,responseTimeSec){if(correct){return confidence===1?{type:'low_confidence_correct',label:'ตอบถูกแต่ยังไม่มั่นใจ',reason:'ควรยืนยันด้วยข้อใหม่ ไม่ถือว่า mastery จากข้อเดียว',hint:null}:{type:'no_error_observed',label:'ยังไม่พบ error ในข้อนี้',reason:'เป็นหลักฐานหนึ่งจุดเท่านั้น',hint:null}}if(q.wrong&&q.wrong[selected]){const [type,reason,hint]=q.wrong[selected];return{type,label:type,reason,hint}}if(confidence===3)return{type:'misconception_risk',label:'เสี่ยง misconception',reason:'ตอบผิดพร้อมความมั่นใจสูง',hint:'ตรวจหลักการก่อนคำนวณซ้ำ'};if(responseTimeSec>=45)return{type:'strategy_or_processing_gap',label:'อาจติดที่ strategy/process',reason:'ตอบผิดและใช้เวลาค่อนข้างนาน',hint:'แบ่งโจทย์เป็นขั้นเล็กลง'};if(confidence===1)return{type:'knowledge_or_prerequisite_gap',label:'อาจมี knowledge/prerequisite gap',reason:'ตอบผิดพร้อม confidence ต่ำ',hint:'ทบทวนแนวคิดพื้นฐานก่อน'};return{type:'procedural_gap',label:'อาจมี procedural gap',reason:'ตอบผิด แต่หลักฐานยังไม่พอชี้เฉพาะจุด',hint:'ทำ worked example ทีละขั้น'}}
function submitCurrent(){const q=currentQuestion(),selected=answers[idx],confidence=conf[idx];if(selected==null||confidence==null){alert('เลือกคำตอบและบอกระดับความมั่นใจก่อนนะ');return}const correct=selected===q.a,rt=Math.max(0,Math.round((Date.now()-itemStartAt)/1000));log('item_answer',{skill:active.id,phase,questionId:q.id,selected:String.fromCharCode(65+selected),correct,confidence,responseTimeSec:rt,hintLevel:phase==='rescue'?rescueHintLevel:0});const d=diagnose(q,selected,correct,confidence,rt);log('diagnosis_generated',{skill:active.id,phase,questionId:q.id,diagnosisType:d.type,reason:d.reason,evidenceStrength:'DEV_HEURISTIC'});if(phase==='diagnostic'){diagnosticEvidence={correct,confidence,responseTimeSec:rt,diagnosis:d};showLesson();return}if(phase==='fresh'){if(!correct)wrongEvidence.push({questionId:q.id,diagnosis:d,confidence,responseTimeSec:rt});if(idx<active.fresh.length-1){idx++;beginItem();return}finishFresh();return}if(phase==='rescue'){finishRescue(correct,confidence,rt,d);return}if(phase==='post_support'){finishPostSupport(correct,confidence,rt,d)}}
function showLesson(){phase='teach';const d=diagnosticEvidence.diagnosis;const opener=diagnosticEvidence.correct?'เริ่มได้ดี เราจะใช้ข้อใหม่เช็กให้แน่ใจอีกที':'เจอร่องรอยแล้ว! ไม่ใช่เรื่องน่ากลัว — ยิ่งรู้จุดติดเร็ว ยิ่งแก้ได้เร็ว';document.getElementById('pane').innerHTML=`<h1>${esc(active.title)}</h1><div class="coach"><strong>${opener}</strong><br>${esc(diagnosisThai(d))}</div><div class="lesson"><h3>เคล็ดลับรอบนี้</h3><p>${esc(active.lesson)}</p><div class="visual">${esc(active.visual)}</div><a class="resource" target="_blank" rel="noopener" href="${active.url}" onclick="log('resource_open',{skill:active.id,url:active.url,phase:'teach'})">↗ ดูตัวอย่างเพิ่มเติมจาก สสวท.</a><div class="actions"><button class="btn primary" onclick="beginFresh()">พร้อมแล้ว ลุยโจทย์ใหม่</button><button class="btn light" onclick="exitModule()">พักก่อน</button></div></div>${devDetails(`diagnosis=${d.type} • reason=${esc(d.reason)} • evidence=DEV_HEURISTIC`)}`}
function beginFresh(){phase='fresh';idx=0;answers=[];conf=[];wrongEvidence=[];log('fresh_start',{skill:active.id});beginItem()}
function finishFresh(){let correct=0;active.fresh.forEach((q,i)=>{if(answers[i]===q.a)correct++});const freshScore=Math.round(correct/active.fresh.length*100),avg=(conf.reduce((a,b)=>a+b,0)/conf.length).toFixed(2);log('fresh_complete',{skill:active.id,freshScore,avgConfidence:avg,wrongCount:wrongEvidence.length});if(wrongEvidence.length===0){completeModule({freshScore,avgConfidence:avg,rescueScore:null,postSupportFreshScore:null,rescueUsed:false});return}const lead=wrongEvidence.find(x=>x.confidence===3)||wrongEvidence[0];state._pending={freshScore,avgConfidence:avg};save();document.getElementById('pane').innerHTML=`<h1>จับได้แล้ว 1 จุด 🧩</h1><div class="feedback"><strong>รอบนี้ทำเองได้ ${Math.round(freshScore/100*active.fresh.length)} จาก ${active.fresh.length} ข้อ</strong><br><span class="muted">คะแนนก่อนรับความช่วยเหลือถูกเก็บไว้แล้ว ไม่เอาคะแนนหลังช่วยมาปน</span></div><div class="coach"><strong>จุดที่น่าลองแกะ:</strong> ${esc(diagnosisThai(lead.diagnosis))}<br>ดีเลย — ตอนนี้เรารู้แล้วว่าต้องลงแรงตรงไหน แทนที่จะอ่านใหม่ทั้งบท 😄</div><div class="actions"><button class="btn primary" onclick="beginRescue()">ไปแกะจุดนี้กัน</button><button class="btn light" onclick="exitModule()">พักก่อน</button></div>${devDetails(`Fresh=${freshScore}% • avgConfidence=${avg} • diagnosis=${lead.diagnosis.type} • Fresh score locked`)}`}
function beginRescue(){phase='rescue';idx=0;answers=[];conf=[];rescueHintLevel=0;log('rescue_start',{skill:active.id,wrongFreshItems:wrongEvidence.map(x=>x.questionId)});beginItem()}
function finishRescue(correct,confidence,rt,diagnosis){const pending=state._pending||{freshScore:0,avgConfidence:String(confidence)};state._pending={...pending,rescueScore:correct?100:0,rescueConfidence:confidence,hintsUsed:rescueHintLevel};log('rescue_complete',{skill:active.id,rescueScore:state._pending.rescueScore,confidence,responseTimeSec:rt,hintsUsed:rescueHintLevel,diagnosisType:diagnosis.type});save();beginPostSupport()}
function beginPostSupport(){phase='post_support';idx=0;answers=[];conf=[];log('post_support_fresh_start',{skill:active.id,questionId:active.retest.id});beginItem()}
function finishPostSupport(correct,confidence,rt,diagnosis){const p=state._pending||{freshScore:0,avgConfidence:String(confidence),rescueScore:null,hintsUsed:0};const postSupportFreshScore=correct?100:0;log('post_support_fresh_complete',{skill:active.id,postSupportFreshScore,confidence,responseTimeSec:rt,diagnosisType:diagnosis.type});delete state._pending;completeModule({freshScore:p.freshScore,avgConfidence:p.avgConfidence,rescueScore:p.rescueScore,postSupportFreshScore,rescueUsed:true,hintsUsed:p.hintsUsed})}
function completeModule({freshScore,avgConfidence,rescueScore,postSupportFreshScore,rescueUsed,hintsUsed=0}){const status=deriveSkillStatus(freshScore,rescueScore,postSupportFreshScore),eligibleForRetention=status!=='NEEDS_FOUNDATION_REPAIR',schedule=eligibleForRetention?retentionSchedule():[],nextAction=eligibleForRetention?'RETENTION':'DEEP_REPAIR',completedAt=nowIso();state.done[active.id]=true;state.results[active.id]={freshScore,rescueScore,postSupportFreshScore,avgConfidence,at:completedAt,label:'DEV session evidence'};state.skillState[active.id]={status,freshAccuracy:freshScore,rescueAccuracy:rescueScore,postSupportFreshAccuracy:postSupportFreshScore,diagnosticCorrect:diagnosticEvidence?.correct??null,avgConfidence:Number(avgConfidence),supportUsed:rescueUsed,hintsUsed,lastEvidenceAt:completedAt,retentionEligible:eligibleForRetention,retentionDue:schedule,nextAction};state.attemptHistory.push({attemptId:currentAttemptId,skill:active.id,startedAt:attemptStartedAt,completedAt,diagnostic:diagnosticEvidence,freshScore,rescueScore,postSupportFreshScore,avgConfidence:Number(avgConfidence),supportUsed:rescueUsed,hintsUsed,status,retentionEligible:eligibleForRetention,nextAction});log('skill_state_update',{skill:active.id,status,freshAccuracy:freshScore,rescueAccuracy:rescueScore,postSupportFreshAccuracy:postSupportFreshScore,supportUsed:rescueUsed,retentionEligible:eligibleForRetention,nextAction});if(eligibleForRetention)log('retention_scheduled',{skill:active.id,schedule});else log('deep_repair_required',{skill:active.id,reason:'post_support_transfer_not_passed'});save();phase='completed';const headline=status==='TENTATIVE_MASTERY_DEV'?'ผ่านด่านนี้แบบไม่ต้องช่วย 🎯':status==='TRANSFER_AFTER_SUPPORT_DEV'?'เยี่ยม! วิธีที่ทบทวนไปเริ่มติดมือแล้ว 💪':'ยังไม่จบเกม — แต่ตอนนี้เรารู้ว่าจะซ่อมตรงไหน 🧩';const body=status==='TENTATIVE_MASTERY_DEV'?'รอบนี้ทำโจทย์ใหม่ได้ครบด้วยตัวเอง ต่อไปเราจะกลับมาเช็กภายหลังว่ายังจำได้จริงไหม':status==='TRANSFER_AFTER_SUPPORT_DEV'?'หลังทบทวนแล้ว คุณทำโจทย์ใหม่อีกข้อได้ด้วยตัวเอง นี่คือหลักฐานที่ดีกว่าการทำข้อเดิมซ้ำ':'คุณทำแบบฝึกช่วยได้ แต่โจทย์ใหม่หลังทบทวนยังไม่ผ่าน เราจะไม่รีบส่งไปทบทวนระยะยาว — รอบถัดไปต้องซ่อมพื้นฐานให้แน่นก่อน';const scoreLine=`ทำเองก่อนช่วย: ${freshScore}%${rescueScore==null?'':` • รอบช่วย: ${rescueScore}%`}${postSupportFreshScore==null?'':` • โจทย์ใหม่หลังทบทวน: ${postSupportFreshScore}%`}`;document.getElementById('pane').innerHTML=`<h1>${headline}</h1><div class="feedback"><strong>${scoreLine}</strong></div><div class="coach">${body}</div><div class="next"><strong>ก้าวต่อไป</strong><br>${eligibleForRetention?'เราจะนัดกลับมาเช็กความจำเป็นระยะ 1 / 3 / 7 / 21 วัน':'กลับไปฝึกพื้นฐานจุดนี้ด้วยวิธีที่ช้าลงและชัดขึ้น แล้วค่อยลองโจทย์ใหม่อีกครั้ง'}</div><div class="actions"><button class="btn primary" onclick="finishAndHome()">กลับไปเลือกภารกิจ</button></div>${devDetails(`Fresh=${freshScore}% • Rescue=${rescueScore} • Post=${postSupportFreshScore} • state=${status} • nextAction=${nextAction} • retentionEligible=${eligibleForRetention}`)}`}
function finishAndHome(){active=null;phase='idle';currentAttemptId=null;attemptStartedAt=null;renderHome()}
function showTelemetry(){hideAll();document.getElementById('telemetry').classList.remove('hidden');document.getElementById('telemetryBox').textContent=JSON.stringify({skillState:state.skillState,attemptHistory:state.attemptHistory,events:state.events},null,2)}
function copySummary(){const lines=['KIDS LEARNING HUB — ADULT DEV TESTER',state.profile.tester+' • '+state.profile.scenario,'App '+APP_VERSION,''];SKILLS.forEach(s=>{const r=state.results[s.id],ss=state.skillState[s.id];lines.push(s.title+': '+(r?`Fresh ${r.freshScore}% | Rescue ${r.rescueScore==null?'n/a':r.rescueScore+'%'} | Post ${r.postSupportFreshScore==null?'n/a':r.postSupportFreshScore+'%'} | ${ss?.status||'no state'} | next ${ss?.nextAction||'n/a'}`:'pending'))});lines.push('Attempts: '+state.attemptHistory.length,'Events: '+state.events.length);const text=lines.join('\n');navigator.clipboard?.writeText(text).then(()=>alert('คัดลอกผลแล้ว')).catch(()=>prompt('คัดลอกข้อความนี้',text))}
function downloadJSON(){const blob=new Blob([JSON.stringify({meta:{mode:'ADULT_DEV_TEST',appVersion:APP_VERSION,contentVersion:CONTENT_VERSION,eventSchemaVersion:EVENT_SCHEMA_VERSION,exportedAt:nowIso()},profile:state.profile,results:state.results,skillState:state.skillState,attemptHistory:state.attemptHistory,events:state.events},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='klh_dev_tester_session_v051.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function resetApp(){if(confirm('ล้างข้อมูลทดสอบใน browser เครื่องนี้?')){localStorage.removeItem(KEY);LEGACY_KEYS.forEach(k=>localStorage.removeItem(k));location.reload()}}
if(state.profile)renderHome();
